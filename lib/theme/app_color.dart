import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF00512D); // Corrected Orange
  static const Color primary = Colors.orange; // Corrected Black
  static const Color text = Colors.white; // White
  static const Color secondary = Colors.black;
  static const Color test = Colors.black; // Corrected Black
  // Add more colors as needed
}
